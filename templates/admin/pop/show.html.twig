{% extends 'base.html.twig' %}

{% block title %}Pop{% endblock %}

{% block body %}
    <h1>Pop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pop.name }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ pop.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pop_index') }}">back to list</a>

    <a href="{{ path('app_pop_edit', {'id': pop.id}) }}">edit</a>

    {{ include('admin/pop/_delete_form.html.twig') }}
{% endblock %}
